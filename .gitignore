# Logs
logs
*.log

# Xcode / Swift
# Unanchored: the release workflow builds to ../DerivedData from Minidown/, so it lands at the
# repo root, not under Minidown/ where a local `xcodebuild` would put it.
DerivedData/
**/xcuserdata/
*.xcuserstate
*.xccheckout
*.xcscmblueprint
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
.build/

# Editor directories and files
.idea
.DS_Store
*.sw?
.claude/

# Generated by XcodeGen from project.yml — edit the spec, not these
Minidown/Minidown/Info.plist

# Release artifacts, built at the repo root by .github/workflows/release.yml
minidown-*.zip
minidown-*.dmg
